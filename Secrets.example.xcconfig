// Copy to Secrets.xcconfig (gitignored) and fill in real values.
//
// Nothing in this file may ever be committed with a real key in it. The app
// builds and runs with the placeholders below — AdService falls back to
// StubAdService whenever the unit id is missing or still the test value.

// Google's public test unit. Safe to ship in a debug build; never in a release one.
ADMOB_APP_ID = ca-app-pub-3940256099942544~1458002511
ADMOB_REWARDED_UNIT_ID = ca-app-pub-3940256099942544/1712485313

// Apple team identifier, only needed for device builds.
DEVELOPMENT_TEAM =
